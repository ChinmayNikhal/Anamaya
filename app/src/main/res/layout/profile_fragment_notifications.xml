<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f2f2f2"
    android:padding="16dp"
    tools:context=".profile.FragmentNotifications">

    <TextView
        android:id="@+id/notifications_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Notifications"
        android:textColor="@color/app_primary_teal"
        android:textSize="24sp"
        android:textStyle="bold"
        android:fontFamily="@font/roboto_mono"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:layout_alignParentTop="true"/>

    <ScrollView
        android:id="@+id/notifications_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/notifications_title"
        android:layout_above="@+id/dismiss_all_button"
        android:layout_marginBottom="16dp">

        <LinearLayout
            android:id="@+id/notifications_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp"/>

    </ScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/dismiss_all_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Dismiss All Notifications"
        android:fontFamily="@font/roboto_mono"
        android:paddingVertical="12dp"
        android:textSize="18sp"
        android:textColor="@color/app_content_white"
        app:backgroundTint="@color/app_primary_teal"
        app:cornerRadius="8dp" />

</RelativeLayout>
